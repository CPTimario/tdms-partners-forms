# Required environment variables
# `DEEPLINK_KEY` is used to derive an AES-256-GCM key for encrypting/decrypting deeplink tokens.
# Keep this secret (set via CI/project settings) and rotate if compromised.
DEEPLINK_KEY=replace-with-a-secret-key
